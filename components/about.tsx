"use client";

import React from "react";
import SectionHeading from "./section-heading";
import { motion } from "framer-motion";
import { useSectionInView } from "@/lib/hooks";

export default function About() {
  const { ref } = useSectionInView("About");

  return (
    <motion.section
      ref={ref}
      className="mb-28 max-w-[45rem] mt-10  text-center leading-8 sm:mb-40 scroll-mt-28"
      initial={{ opacity: 0, y: 100 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 0.175 }}
      id="about"
    >
      <SectionHeading>About me</SectionHeading>
      <p>
        With over 2 years of experience in frontend development, I specialize in building high-performance web
        applications using NextJS and ReactJS. My projects have consistently improved productivity and user
        satisfaction. I have been teaching since 2023 and have pedagogical potential.
      </p>
    </motion.section>
  );
}
